package core;

public class Config {
  static String FRAME_NAME    = "Bot Trust Problem by Gurvinderjeet Singh"; // Title for JFrame
  static int    FRAME_WIDTH   = 900; // Min width for JFrame
  static int    FRAME_HEIGHT  = 500; // Min height for JFrame
  static int    MIN_LOCATION  = 1;   // Min size where bot can move
  static int    MAX_LOCATION  = 100; // Max size where bot can move
  static int    DEFAULT_SPEED = 1;   // Min speed project can run
  static int    MAX_SPEED     = 40;  // Max speed project can run
}
